app:
  jwt:
    secret: test-only-signing-key-with-enough-length-for-hs256
    expiration-minutes: 60
  refresh-token:
    expiration-days: 30
  mail:
    from: test@tasknest.ba
  cors:
    allowed-origins: http://localhost:4200
  task-expiry:
    # Testovi zovu expireOverdueTasks() direktno; raspored bi im u pozadini
    # mijenjao podatke i pravio nasumicne padove.
    enabled: false

spring:
  jpa:
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    org.hibernate.SQL: warn
    liquibase: warn
