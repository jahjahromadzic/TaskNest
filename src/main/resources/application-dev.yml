app:
  jwt:
    secret: ${JWT_SECRET:dev-only-insecure-signing-key-do-not-use-in-production}

spring:
  jpa:
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
